{
  "name": "ndmathur-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd ../client && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "@sendgrid/mail": "^7.7.0"
  }
}
